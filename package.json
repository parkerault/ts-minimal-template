{
  "name": "ts-minimal-template",
  "version": "1.0.0",
  "description": "A minimal template for TypeScript doodles.",
  "scripts": {
    "start": "ts-node src/index.ts",
    "start:watch": "nodemon src/index.ts --watch ./src",
    "test": "jest",
    "test:watch": "jest --watch",
    "build": "rm -r ./dist && tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serve": "^11.3.2",
    "ts-node": "^9.1.1"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.6",
    "typescript": "^4.3.0-beta"
  },
  "jest": {
    "preset": "ts-jest"
  }
}
